<Page
    x:Class="OpenHeroSelectGUI.Tab_MUA"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d">

    <!--<Page.KeyboardAccelerators>
        <KeyboardAccelerator Key="F5"/>
    </Page.KeyboardAccelerators>-->

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MaxWidth="240"/>
            <ColumnDefinition/>
            <ColumnDefinition MaxWidth="300"/>
        </Grid.ColumnDefinitions>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" AllowDrop="True" DragOver="AvailableCharacters_DragOver" Drop="AvailableCharacters_Drop">
            <Grid.RowDefinitions>
                <RowDefinition Height="35"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button x:Name="BtnReload" Click="BtnReload_Click" HorizontalAlignment="Right" VerticalAlignment="Top" ToolTipService.ToolTip="F5 | Reload the available characters">
                <SymbolIcon>Refresh</SymbolIcon>
                <Button.KeyboardAccelerators>
                    <KeyboardAccelerator Key="F5" />
                </Button.KeyboardAccelerators>
            </Button>
            <Frame x:Name="AvailableCharacters" Grid.Row="1"/>
        </Grid>
        <Grid Grid.Column="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="140"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid x:Name="MUASettings" HorizontalAlignment="Left">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition MaxWidth="110"/>
                    <ColumnDefinition/>
                    <ColumnDefinition MinWidth="500"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <!--<CheckBox/>-->
                    <!--<Slider/>-->
                    <TextBlock x:Name="CharLimit" Text="Character Limit"/>
                    <ToggleSwitch x:Name="RosterHackToggle" Header="(Roster Hack)" DataContext="Tab_Settings" IsOn="{x:Bind Cfg.MUA.RosterHack, Mode=TwoWay}" Toggled="RosterHack_Toggled" OffContent="27" OnContent="50"/>
                    <ToggleSwitch x:Name="UnlockToggle" Header="Unlocks" IsOn="{x:Bind Cfg.MUA.Unlocker, Mode=TwoWay}" OffContent="Off" OnContent="Update"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="0 5 10 0" VerticalAlignment="Top">
                    <Button x:Name="BtnRunGame" Click="BtnRunGame_Click" Margin="0 0 0 25" Padding="10" ToolTipService.ToolTip="Run Marvel: Ultimate Alliance only, without running OHS">
                        <TextBlock TextAlignment="Center">Run<LineBreak/>Marvel<LineBreak/>Ultimate Alliance</TextBlock>
                    </Button>
                    <Button x:Name="BtnUnlockAll" Content="Unlock All" Click="BtnUnlockAll_Click" ToolTipService.ToolTip="Check all selected characters to be unlocked"/>
                </StackPanel>
                <StackPanel Grid.Column="2">
                    <StackPanel Margin="0 10 0 0" Orientation="Horizontal">
                        <ComboBox x:Name="LayoutBox" Header="Stage Layout" ItemsSource="{x:Bind Layouts, Mode=OneWay}" SelectionChanged="LayoutBox_SelectionChanged" SelectedValuePath="Content" SelectedIndex="{x:Bind Cfg.Dynamic.SelectedLayout}" MinWidth="150"/>
                        <Button x:Name="RefreshLayouts" Click="RefreshLayouts_Click" VerticalAlignment="Bottom" HorizontalAlignment="Right" ToolTipService.ToolTip="Reload layouts">
                            <SymbolIcon>Refresh</SymbolIcon>
                        </Button>
                    </StackPanel>
                    <StackPanel Margin="0 10 0 0" Orientation="Horizontal">
                        <ComboBox x:Name="StagesBox" Header="Stage  Model" ItemsSource="{x:Bind Stages, Mode=OneWay}" SelectionChanged="StagesBox_SelectionChanged" SelectedIndex="{x:Bind Cfg.Dynamic.SelectedModel, Mode=OneWay}" MinWidth="150"/>
                        <Button x:Name="RefreshStages" Click="RefreshStages_Click" VerticalAlignment="Bottom" HorizontalAlignment="Right" ToolTipService.ToolTip="Reload stages (does not update until the layout changes)">
                            <SymbolIcon>Refresh</SymbolIcon>
                        </Button>
                    </StackPanel>
                </StackPanel>
            </Grid>
            <Grid x:Name="ImagePanel" Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition x:Name="LayoutHeight"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Image Grid.Row="1" x:Name="StageImage" VerticalAlignment="Bottom"/>
            </Grid>
            <Grid x:Name="LayoutPanel" Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>
                <Grid x:Name="Locations" Margin="0 5 0 0" HorizontalAlignment="Center" VerticalAlignment="Top"/>
                <StackPanel Grid.Row="1" Padding="5" Background="{ThemeResource ButtonBackgroundThemeBrush}">
                    <TextBlock x:Name="LayoutDetails"/>
                    <TextBlock x:Name="StageDetails"/>
                </StackPanel>
            </Grid>
        </Grid>
        <Grid x:Name="SelectedColumn" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <Canvas Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" AllowDrop="True" DragOver="SelectedCharacters_DragOver" Drop="SelectedCharacters_Drop"/>
            <Frame x:Name="SelectedCharacters" ProcessKeyboardAccelerators="SelectedCharacters_Delete">
                <Frame.KeyboardAccelerators>
                    <KeyboardAccelerator Key="Delete" />
                </Frame.KeyboardAccelerators>
            </Frame>
            <StackPanel x:Name="MUAButtons" Grid.Row="1" Orientation="Horizontal">
                <Button x:Name="BtnDefault" Content="Default" Click="MUA_LoadDefault" ToolTipService.ToolTip="Load the default roster"/>
                <Button x:Name="BtnLoadRoster" Content="Load" Click="MUA_LoadRoster" ToolTipService.ToolTip="Load roster from an OHS .cfg file in the rosters folder - Matches characters top-down > left-right, unless the menulocations have been customized"/>
                <Button x:Name="BtnRandom" Content="Random" Click="MUA_Random" ToolTipService.ToolTip="Create a random roster from the available characters"/>
                <Button x:Name="BtnClear" Content="Clear" Click="MUA_Clear" ToolTipService.ToolTip="Clear the entire selected roster"/>
                <TextBlock x:Name="RosterSizeCurrent" Text="{x:Bind Cfg.Roster.Count, Mode=OneWay}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="5 0 0 0"/>
                <TextBlock Text="/" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                <TextBlock x:Name="RosterSizeTotal" Text="{x:Bind Cfg.Roster.Total, Mode=OneWay}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
        <controls:ContentDialog x:Name="WarningDialog" Title="Information" CloseButtonText="OK"/>
    </Grid>
</Page>
